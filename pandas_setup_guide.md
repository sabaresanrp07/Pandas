# Before this install python then run these cmd in powershell

- python -m pip install --upgrade pip
- python -m pip install pandas
- python -c "import pandas as pd; print(pd.__version__)" (if u see version then u have installed succefully)

